name: ピル手帳アラーム

"on":
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: テスト通知
        run: |
          curl -s \
            -H "Title: ピル手帳テスト" \
            -H "Tags: pill" \
            -d "テスト通知です" \
            https://ntfy.sh/pill-alarm-pimsilencer
